# config/10-logging.conf

# Send logs to stdout (visible via `docker compose logs`)
log_path = /dev/stdout
info_log_path = /dev/stdout
debug_log_path = /dev/stdout

# Be verbose for debugging
auth_verbose = yes
auth_verbose_passwords = sha1   # hides full password, shows hash
mail_debug = yes
